let name = prompt("Атыңды жаз!") 
let language = prompt("Қай тілде сөйлейсіз? English,Kazak,Russia") 
 
if (language=="English") { 
    function hello(compareWords) { 
         
        return name==language      
} 
alert(`Hello ${name} how are you?`) 
 
} 
        
if (language=="Kazakh") { 
        function hello(compareWords) { 
         
                return name==language  
} 
alert(`${name}Асалаумагалейкум,Қалыңыз қалай?`) 
} 
if (language=="Russia") { 
        
        function hello(compareWords) { 
        return name==language } 
    {
        alert(`${name} Привет как дела?`) 
   }          
        
                
}

else{ 
    alert(`${name} `)     
}
